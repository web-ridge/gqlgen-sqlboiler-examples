// Code generated by SQLBoiler 4.2.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testRecipeBatchNotes(t *testing.T) {
	t.Parallel()

	query := RecipeBatchNotes()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testRecipeBatchNotesDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testRecipeBatchNotesQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := RecipeBatchNotes().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testRecipeBatchNotesSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := RecipeBatchNoteSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testRecipeBatchNotesExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := RecipeBatchNoteExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if RecipeBatchNote exists: %s", err)
	}
	if !e {
		t.Errorf("Expected RecipeBatchNoteExists to return true, but got false.")
	}
}

func testRecipeBatchNotesFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	recipeBatchNoteFound, err := FindRecipeBatchNote(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if recipeBatchNoteFound == nil {
		t.Error("want a record, got nil")
	}
}

func testRecipeBatchNotesBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = RecipeBatchNotes().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testRecipeBatchNotesOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := RecipeBatchNotes().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testRecipeBatchNotesAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	recipeBatchNoteOne := &RecipeBatchNote{}
	recipeBatchNoteTwo := &RecipeBatchNote{}
	if err = randomize.Struct(seed, recipeBatchNoteOne, recipeBatchNoteDBTypes, false, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}
	if err = randomize.Struct(seed, recipeBatchNoteTwo, recipeBatchNoteDBTypes, false, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = recipeBatchNoteOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = recipeBatchNoteTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := RecipeBatchNotes().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testRecipeBatchNotesCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	recipeBatchNoteOne := &RecipeBatchNote{}
	recipeBatchNoteTwo := &RecipeBatchNote{}
	if err = randomize.Struct(seed, recipeBatchNoteOne, recipeBatchNoteDBTypes, false, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}
	if err = randomize.Struct(seed, recipeBatchNoteTwo, recipeBatchNoteDBTypes, false, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = recipeBatchNoteOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = recipeBatchNoteTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func recipeBatchNoteBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *RecipeBatchNote) error {
	*o = RecipeBatchNote{}
	return nil
}

func recipeBatchNoteAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *RecipeBatchNote) error {
	*o = RecipeBatchNote{}
	return nil
}

func recipeBatchNoteAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *RecipeBatchNote) error {
	*o = RecipeBatchNote{}
	return nil
}

func recipeBatchNoteBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *RecipeBatchNote) error {
	*o = RecipeBatchNote{}
	return nil
}

func recipeBatchNoteAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *RecipeBatchNote) error {
	*o = RecipeBatchNote{}
	return nil
}

func recipeBatchNoteBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *RecipeBatchNote) error {
	*o = RecipeBatchNote{}
	return nil
}

func recipeBatchNoteAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *RecipeBatchNote) error {
	*o = RecipeBatchNote{}
	return nil
}

func recipeBatchNoteBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *RecipeBatchNote) error {
	*o = RecipeBatchNote{}
	return nil
}

func recipeBatchNoteAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *RecipeBatchNote) error {
	*o = RecipeBatchNote{}
	return nil
}

func testRecipeBatchNotesHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &RecipeBatchNote{}
	o := &RecipeBatchNote{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, false); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote object: %s", err)
	}

	AddRecipeBatchNoteHook(boil.BeforeInsertHook, recipeBatchNoteBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	recipeBatchNoteBeforeInsertHooks = []RecipeBatchNoteHook{}

	AddRecipeBatchNoteHook(boil.AfterInsertHook, recipeBatchNoteAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	recipeBatchNoteAfterInsertHooks = []RecipeBatchNoteHook{}

	AddRecipeBatchNoteHook(boil.AfterSelectHook, recipeBatchNoteAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	recipeBatchNoteAfterSelectHooks = []RecipeBatchNoteHook{}

	AddRecipeBatchNoteHook(boil.BeforeUpdateHook, recipeBatchNoteBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	recipeBatchNoteBeforeUpdateHooks = []RecipeBatchNoteHook{}

	AddRecipeBatchNoteHook(boil.AfterUpdateHook, recipeBatchNoteAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	recipeBatchNoteAfterUpdateHooks = []RecipeBatchNoteHook{}

	AddRecipeBatchNoteHook(boil.BeforeDeleteHook, recipeBatchNoteBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	recipeBatchNoteBeforeDeleteHooks = []RecipeBatchNoteHook{}

	AddRecipeBatchNoteHook(boil.AfterDeleteHook, recipeBatchNoteAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	recipeBatchNoteAfterDeleteHooks = []RecipeBatchNoteHook{}

	AddRecipeBatchNoteHook(boil.BeforeUpsertHook, recipeBatchNoteBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	recipeBatchNoteBeforeUpsertHooks = []RecipeBatchNoteHook{}

	AddRecipeBatchNoteHook(boil.AfterUpsertHook, recipeBatchNoteAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	recipeBatchNoteAfterUpsertHooks = []RecipeBatchNoteHook{}
}

func testRecipeBatchNotesInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testRecipeBatchNotesInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(recipeBatchNoteColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testRecipeBatchNoteToOneRecipeBatchUsingBatch(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local RecipeBatchNote
	var foreign RecipeBatch

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, recipeBatchNoteDBTypes, false, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, recipeBatchDBTypes, false, recipeBatchColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatch struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	local.BatchID = foreign.ID
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Batch().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if check.ID != foreign.ID {
		t.Errorf("want: %v, got %v", foreign.ID, check.ID)
	}

	slice := RecipeBatchNoteSlice{&local}
	if err = local.L.LoadBatch(ctx, tx, false, (*[]*RecipeBatchNote)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Batch == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Batch = nil
	if err = local.L.LoadBatch(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Batch == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testRecipeBatchNoteToOneSetOpRecipeBatchUsingBatch(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a RecipeBatchNote
	var b, c RecipeBatch

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, recipeBatchNoteDBTypes, false, strmangle.SetComplement(recipeBatchNotePrimaryKeyColumns, recipeBatchNoteColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, recipeBatchDBTypes, false, strmangle.SetComplement(recipeBatchPrimaryKeyColumns, recipeBatchColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, recipeBatchDBTypes, false, strmangle.SetComplement(recipeBatchPrimaryKeyColumns, recipeBatchColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*RecipeBatch{&b, &c} {
		err = a.SetBatch(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Batch != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.BatchRecipeBatchNotes[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if a.BatchID != x.ID {
			t.Error("foreign key was wrong value", a.BatchID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.BatchID))
		reflect.Indirect(reflect.ValueOf(&a.BatchID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if a.BatchID != x.ID {
			t.Error("foreign key was wrong value", a.BatchID, x.ID)
		}
	}
}

func testRecipeBatchNotesReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testRecipeBatchNotesReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := RecipeBatchNoteSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testRecipeBatchNotesSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := RecipeBatchNotes().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	recipeBatchNoteDBTypes = map[string]string{`ID`: `integer`, `CreatedAt`: `timestamp with time zone`, `UpdatedAt`: `timestamp with time zone`, `DeletedAt`: `timestamp with time zone`, `Note`: `text`, `Link`: `character varying`, `BatchID`: `integer`}
	_                      = bytes.MinRead
)

func testRecipeBatchNotesUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(recipeBatchNotePrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(recipeBatchNoteAllColumns) == len(recipeBatchNotePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNotePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testRecipeBatchNotesSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(recipeBatchNoteAllColumns) == len(recipeBatchNotePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &RecipeBatchNote{}
	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNoteColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, recipeBatchNoteDBTypes, true, recipeBatchNotePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(recipeBatchNoteAllColumns, recipeBatchNotePrimaryKeyColumns) {
		fields = recipeBatchNoteAllColumns
	} else {
		fields = strmangle.SetComplement(
			recipeBatchNoteAllColumns,
			recipeBatchNotePrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := RecipeBatchNoteSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testRecipeBatchNotesUpsert(t *testing.T) {
	t.Parallel()

	if len(recipeBatchNoteAllColumns) == len(recipeBatchNotePrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := RecipeBatchNote{}
	if err = randomize.Struct(seed, &o, recipeBatchNoteDBTypes, true); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert RecipeBatchNote: %s", err)
	}

	count, err := RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, recipeBatchNoteDBTypes, false, recipeBatchNotePrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize RecipeBatchNote struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert RecipeBatchNote: %s", err)
	}

	count, err = RecipeBatchNotes().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
