// Code generated by SQLBoiler 4.2.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// AuthUserUserPermission is an object representing the database table.
type AuthUserUserPermission struct {
	ID           int `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID       int `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	PermissionID int `boil:"permission_id" json:"permission_id" toml:"permission_id" yaml:"permission_id"`

	R *authUserUserPermissionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L authUserUserPermissionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var AuthUserUserPermissionColumns = struct {
	ID           string
	UserID       string
	PermissionID string
}{
	ID:           "id",
	UserID:       "user_id",
	PermissionID: "permission_id",
}

// Generated where

var AuthUserUserPermissionWhere = struct {
	ID           whereHelperint
	UserID       whereHelperint
	PermissionID whereHelperint
}{
	ID:           whereHelperint{field: "\"auth_user_user_permissions\".\"id\""},
	UserID:       whereHelperint{field: "\"auth_user_user_permissions\".\"user_id\""},
	PermissionID: whereHelperint{field: "\"auth_user_user_permissions\".\"permission_id\""},
}

// AuthUserUserPermissionRels is where relationship names are stored.
var AuthUserUserPermissionRels = struct {
	Permission string
	User       string
}{
	Permission: "Permission",
	User:       "User",
}

// authUserUserPermissionR is where relationships are stored.
type authUserUserPermissionR struct {
	Permission *AuthPermission `boil:"Permission" json:"Permission" toml:"Permission" yaml:"Permission"`
	User       *AuthUser       `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*authUserUserPermissionR) NewStruct() *authUserUserPermissionR {
	return &authUserUserPermissionR{}
}

// authUserUserPermissionL is where Load methods for each relationship are stored.
type authUserUserPermissionL struct{}

var (
	authUserUserPermissionAllColumns            = []string{"id", "user_id", "permission_id"}
	authUserUserPermissionColumnsWithoutDefault = []string{"user_id", "permission_id"}
	authUserUserPermissionColumnsWithDefault    = []string{"id"}
	authUserUserPermissionPrimaryKeyColumns     = []string{"id"}
)

type (
	// AuthUserUserPermissionSlice is an alias for a slice of pointers to AuthUserUserPermission.
	// This should generally be used opposed to []AuthUserUserPermission.
	AuthUserUserPermissionSlice []*AuthUserUserPermission
	// AuthUserUserPermissionHook is the signature for custom AuthUserUserPermission hook methods
	AuthUserUserPermissionHook func(context.Context, boil.ContextExecutor, *AuthUserUserPermission) error

	authUserUserPermissionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	authUserUserPermissionType                 = reflect.TypeOf(&AuthUserUserPermission{})
	authUserUserPermissionMapping              = queries.MakeStructMapping(authUserUserPermissionType)
	authUserUserPermissionPrimaryKeyMapping, _ = queries.BindMapping(authUserUserPermissionType, authUserUserPermissionMapping, authUserUserPermissionPrimaryKeyColumns)
	authUserUserPermissionInsertCacheMut       sync.RWMutex
	authUserUserPermissionInsertCache          = make(map[string]insertCache)
	authUserUserPermissionUpdateCacheMut       sync.RWMutex
	authUserUserPermissionUpdateCache          = make(map[string]updateCache)
	authUserUserPermissionUpsertCacheMut       sync.RWMutex
	authUserUserPermissionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var authUserUserPermissionBeforeInsertHooks []AuthUserUserPermissionHook
var authUserUserPermissionBeforeUpdateHooks []AuthUserUserPermissionHook
var authUserUserPermissionBeforeDeleteHooks []AuthUserUserPermissionHook
var authUserUserPermissionBeforeUpsertHooks []AuthUserUserPermissionHook

var authUserUserPermissionAfterInsertHooks []AuthUserUserPermissionHook
var authUserUserPermissionAfterSelectHooks []AuthUserUserPermissionHook
var authUserUserPermissionAfterUpdateHooks []AuthUserUserPermissionHook
var authUserUserPermissionAfterDeleteHooks []AuthUserUserPermissionHook
var authUserUserPermissionAfterUpsertHooks []AuthUserUserPermissionHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *AuthUserUserPermission) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range authUserUserPermissionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *AuthUserUserPermission) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range authUserUserPermissionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *AuthUserUserPermission) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range authUserUserPermissionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *AuthUserUserPermission) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range authUserUserPermissionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *AuthUserUserPermission) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range authUserUserPermissionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *AuthUserUserPermission) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range authUserUserPermissionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *AuthUserUserPermission) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range authUserUserPermissionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *AuthUserUserPermission) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range authUserUserPermissionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *AuthUserUserPermission) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range authUserUserPermissionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddAuthUserUserPermissionHook registers your hook function for all future operations.
func AddAuthUserUserPermissionHook(hookPoint boil.HookPoint, authUserUserPermissionHook AuthUserUserPermissionHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		authUserUserPermissionBeforeInsertHooks = append(authUserUserPermissionBeforeInsertHooks, authUserUserPermissionHook)
	case boil.BeforeUpdateHook:
		authUserUserPermissionBeforeUpdateHooks = append(authUserUserPermissionBeforeUpdateHooks, authUserUserPermissionHook)
	case boil.BeforeDeleteHook:
		authUserUserPermissionBeforeDeleteHooks = append(authUserUserPermissionBeforeDeleteHooks, authUserUserPermissionHook)
	case boil.BeforeUpsertHook:
		authUserUserPermissionBeforeUpsertHooks = append(authUserUserPermissionBeforeUpsertHooks, authUserUserPermissionHook)
	case boil.AfterInsertHook:
		authUserUserPermissionAfterInsertHooks = append(authUserUserPermissionAfterInsertHooks, authUserUserPermissionHook)
	case boil.AfterSelectHook:
		authUserUserPermissionAfterSelectHooks = append(authUserUserPermissionAfterSelectHooks, authUserUserPermissionHook)
	case boil.AfterUpdateHook:
		authUserUserPermissionAfterUpdateHooks = append(authUserUserPermissionAfterUpdateHooks, authUserUserPermissionHook)
	case boil.AfterDeleteHook:
		authUserUserPermissionAfterDeleteHooks = append(authUserUserPermissionAfterDeleteHooks, authUserUserPermissionHook)
	case boil.AfterUpsertHook:
		authUserUserPermissionAfterUpsertHooks = append(authUserUserPermissionAfterUpsertHooks, authUserUserPermissionHook)
	}
}

// One returns a single authUserUserPermission record from the query.
func (q authUserUserPermissionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*AuthUserUserPermission, error) {
	o := &AuthUserUserPermission{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for auth_user_user_permissions")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all AuthUserUserPermission records from the query.
func (q authUserUserPermissionQuery) All(ctx context.Context, exec boil.ContextExecutor) (AuthUserUserPermissionSlice, error) {
	var o []*AuthUserUserPermission

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to AuthUserUserPermission slice")
	}

	if len(authUserUserPermissionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all AuthUserUserPermission records in the query.
func (q authUserUserPermissionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count auth_user_user_permissions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q authUserUserPermissionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if auth_user_user_permissions exists")
	}

	return count > 0, nil
}

// Permission pointed to by the foreign key.
func (o *AuthUserUserPermission) Permission(mods ...qm.QueryMod) authPermissionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PermissionID),
	}

	queryMods = append(queryMods, mods...)

	query := AuthPermissions(queryMods...)
	queries.SetFrom(query.Query, "\"auth_permission\"")

	return query
}

// User pointed to by the foreign key.
func (o *AuthUserUserPermission) User(mods ...qm.QueryMod) authUserQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	query := AuthUsers(queryMods...)
	queries.SetFrom(query.Query, "\"auth_user\"")

	return query
}

// LoadPermission allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (authUserUserPermissionL) LoadPermission(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAuthUserUserPermission interface{}, mods queries.Applicator) error {
	var slice []*AuthUserUserPermission
	var object *AuthUserUserPermission

	if singular {
		object = maybeAuthUserUserPermission.(*AuthUserUserPermission)
	} else {
		slice = *maybeAuthUserUserPermission.(*[]*AuthUserUserPermission)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &authUserUserPermissionR{}
		}
		args = append(args, object.PermissionID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &authUserUserPermissionR{}
			}

			for _, a := range args {
				if a == obj.PermissionID {
					continue Outer
				}
			}

			args = append(args, obj.PermissionID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`auth_permission`),
		qm.WhereIn(`auth_permission.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AuthPermission")
	}

	var resultSlice []*AuthPermission
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AuthPermission")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for auth_permission")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for auth_permission")
	}

	if len(authUserUserPermissionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Permission = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PermissionID == foreign.ID {
				local.R.Permission = foreign
				break
			}
		}
	}

	return nil
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (authUserUserPermissionL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeAuthUserUserPermission interface{}, mods queries.Applicator) error {
	var slice []*AuthUserUserPermission
	var object *AuthUserUserPermission

	if singular {
		object = maybeAuthUserUserPermission.(*AuthUserUserPermission)
	} else {
		slice = *maybeAuthUserUserPermission.(*[]*AuthUserUserPermission)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &authUserUserPermissionR{}
		}
		args = append(args, object.UserID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &authUserUserPermissionR{}
			}

			for _, a := range args {
				if a == obj.UserID {
					continue Outer
				}
			}

			args = append(args, obj.UserID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`auth_user`),
		qm.WhereIn(`auth_user.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load AuthUser")
	}

	var resultSlice []*AuthUser
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice AuthUser")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for auth_user")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for auth_user")
	}

	if len(authUserUserPermissionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				break
			}
		}
	}

	return nil
}

// SetPermission of the authUserUserPermission to the related item.
// Sets o.R.Permission to related.
// Adds o to related.R.PermissionAuthUserUserPermissions.
func (o *AuthUserUserPermission) SetPermission(ctx context.Context, exec boil.ContextExecutor, insert bool, related *AuthPermission) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"auth_user_user_permissions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"permission_id"}),
		strmangle.WhereClause("\"", "\"", 2, authUserUserPermissionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PermissionID = related.ID
	if o.R == nil {
		o.R = &authUserUserPermissionR{
			Permission: related,
		}
	} else {
		o.R.Permission = related
	}

	if related.R == nil {
		related.R = &authPermissionR{
			PermissionAuthUserUserPermissions: AuthUserUserPermissionSlice{o},
		}
	} else {
		related.R.PermissionAuthUserUserPermissions = append(related.R.PermissionAuthUserUserPermissions, o)
	}

	return nil
}

// SetUser of the authUserUserPermission to the related item.
// Sets o.R.User to related.
// Adds o to related.R.UserAuthUserUserPermissions.
func (o *AuthUserUserPermission) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *AuthUser) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"auth_user_user_permissions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, authUserUserPermissionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &authUserUserPermissionR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &authUserR{
			UserAuthUserUserPermissions: AuthUserUserPermissionSlice{o},
		}
	} else {
		related.R.UserAuthUserUserPermissions = append(related.R.UserAuthUserUserPermissions, o)
	}

	return nil
}

// AuthUserUserPermissions retrieves all the records using an executor.
func AuthUserUserPermissions(mods ...qm.QueryMod) authUserUserPermissionQuery {
	mods = append(mods, qm.From("\"auth_user_user_permissions\""))
	return authUserUserPermissionQuery{NewQuery(mods...)}
}

// FindAuthUserUserPermission retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindAuthUserUserPermission(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*AuthUserUserPermission, error) {
	authUserUserPermissionObj := &AuthUserUserPermission{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"auth_user_user_permissions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, authUserUserPermissionObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from auth_user_user_permissions")
	}

	return authUserUserPermissionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *AuthUserUserPermission) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no auth_user_user_permissions provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(authUserUserPermissionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	authUserUserPermissionInsertCacheMut.RLock()
	cache, cached := authUserUserPermissionInsertCache[key]
	authUserUserPermissionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			authUserUserPermissionAllColumns,
			authUserUserPermissionColumnsWithDefault,
			authUserUserPermissionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(authUserUserPermissionType, authUserUserPermissionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(authUserUserPermissionType, authUserUserPermissionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"auth_user_user_permissions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"auth_user_user_permissions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into auth_user_user_permissions")
	}

	if !cached {
		authUserUserPermissionInsertCacheMut.Lock()
		authUserUserPermissionInsertCache[key] = cache
		authUserUserPermissionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the AuthUserUserPermission.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *AuthUserUserPermission) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	authUserUserPermissionUpdateCacheMut.RLock()
	cache, cached := authUserUserPermissionUpdateCache[key]
	authUserUserPermissionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			authUserUserPermissionAllColumns,
			authUserUserPermissionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update auth_user_user_permissions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"auth_user_user_permissions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, authUserUserPermissionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(authUserUserPermissionType, authUserUserPermissionMapping, append(wl, authUserUserPermissionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update auth_user_user_permissions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for auth_user_user_permissions")
	}

	if !cached {
		authUserUserPermissionUpdateCacheMut.Lock()
		authUserUserPermissionUpdateCache[key] = cache
		authUserUserPermissionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q authUserUserPermissionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for auth_user_user_permissions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for auth_user_user_permissions")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o AuthUserUserPermissionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), authUserUserPermissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"auth_user_user_permissions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, authUserUserPermissionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in authUserUserPermission slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all authUserUserPermission")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *AuthUserUserPermission) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no auth_user_user_permissions provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(authUserUserPermissionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	authUserUserPermissionUpsertCacheMut.RLock()
	cache, cached := authUserUserPermissionUpsertCache[key]
	authUserUserPermissionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			authUserUserPermissionAllColumns,
			authUserUserPermissionColumnsWithDefault,
			authUserUserPermissionColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			authUserUserPermissionAllColumns,
			authUserUserPermissionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert auth_user_user_permissions, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(authUserUserPermissionPrimaryKeyColumns))
			copy(conflict, authUserUserPermissionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"auth_user_user_permissions\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(authUserUserPermissionType, authUserUserPermissionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(authUserUserPermissionType, authUserUserPermissionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert auth_user_user_permissions")
	}

	if !cached {
		authUserUserPermissionUpsertCacheMut.Lock()
		authUserUserPermissionUpsertCache[key] = cache
		authUserUserPermissionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single AuthUserUserPermission record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *AuthUserUserPermission) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no AuthUserUserPermission provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), authUserUserPermissionPrimaryKeyMapping)
	sql := "DELETE FROM \"auth_user_user_permissions\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from auth_user_user_permissions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for auth_user_user_permissions")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q authUserUserPermissionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no authUserUserPermissionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from auth_user_user_permissions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for auth_user_user_permissions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o AuthUserUserPermissionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(authUserUserPermissionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), authUserUserPermissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"auth_user_user_permissions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, authUserUserPermissionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from authUserUserPermission slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for auth_user_user_permissions")
	}

	if len(authUserUserPermissionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *AuthUserUserPermission) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindAuthUserUserPermission(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *AuthUserUserPermissionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := AuthUserUserPermissionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), authUserUserPermissionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"auth_user_user_permissions\".* FROM \"auth_user_user_permissions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, authUserUserPermissionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in AuthUserUserPermissionSlice")
	}

	*o = slice

	return nil
}

// AuthUserUserPermissionExists checks if the AuthUserUserPermission row exists.
func AuthUserUserPermissionExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"auth_user_user_permissions\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if auth_user_user_permissions exists")
	}

	return exists, nil
}
