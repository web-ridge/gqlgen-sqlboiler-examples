// Code generated by SQLBoiler 4.2.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// LipidInventory is an object representing the database table.
type LipidInventory struct {
	ID            int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt     time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt     time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt     null.Time `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`
	PurchaseDate  time.Time `boil:"purchase_date" json:"purchase_date" toml:"purchase_date" yaml:"purchase_date"`
	ExpiryDate    time.Time `boil:"expiry_date" json:"expiry_date" toml:"expiry_date" yaml:"expiry_date"`
	Cost          float64   `boil:"cost" json:"cost" toml:"cost" yaml:"cost"`
	Weight        float64   `boil:"weight" json:"weight" toml:"weight" yaml:"weight"`
	Sap           float64   `boil:"sap" json:"sap" toml:"sap" yaml:"sap"`
	Naoh          float64   `boil:"naoh" json:"naoh" toml:"naoh" yaml:"naoh"`
	Koh           float64   `boil:"koh" json:"koh" toml:"koh" yaml:"koh"`
	GramsPerLiter float64   `boil:"grams_per_liter" json:"grams_per_liter" toml:"grams_per_liter" yaml:"grams_per_liter"`
	LipidID       int       `boil:"lipid_id" json:"lipid_id" toml:"lipid_id" yaml:"lipid_id"`
	SupplierID    int       `boil:"supplier_id" json:"supplier_id" toml:"supplier_id" yaml:"supplier_id"`

	R *lipidInventoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L lipidInventoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var LipidInventoryColumns = struct {
	ID            string
	CreatedAt     string
	UpdatedAt     string
	DeletedAt     string
	PurchaseDate  string
	ExpiryDate    string
	Cost          string
	Weight        string
	Sap           string
	Naoh          string
	Koh           string
	GramsPerLiter string
	LipidID       string
	SupplierID    string
}{
	ID:            "id",
	CreatedAt:     "created_at",
	UpdatedAt:     "updated_at",
	DeletedAt:     "deleted_at",
	PurchaseDate:  "purchase_date",
	ExpiryDate:    "expiry_date",
	Cost:          "cost",
	Weight:        "weight",
	Sap:           "sap",
	Naoh:          "naoh",
	Koh:           "koh",
	GramsPerLiter: "grams_per_liter",
	LipidID:       "lipid_id",
	SupplierID:    "supplier_id",
}

// Generated where

var LipidInventoryWhere = struct {
	ID            whereHelperint
	CreatedAt     whereHelpertime_Time
	UpdatedAt     whereHelpertime_Time
	DeletedAt     whereHelpernull_Time
	PurchaseDate  whereHelpertime_Time
	ExpiryDate    whereHelpertime_Time
	Cost          whereHelperfloat64
	Weight        whereHelperfloat64
	Sap           whereHelperfloat64
	Naoh          whereHelperfloat64
	Koh           whereHelperfloat64
	GramsPerLiter whereHelperfloat64
	LipidID       whereHelperint
	SupplierID    whereHelperint
}{
	ID:            whereHelperint{field: "\"lipid_inventory\".\"id\""},
	CreatedAt:     whereHelpertime_Time{field: "\"lipid_inventory\".\"created_at\""},
	UpdatedAt:     whereHelpertime_Time{field: "\"lipid_inventory\".\"updated_at\""},
	DeletedAt:     whereHelpernull_Time{field: "\"lipid_inventory\".\"deleted_at\""},
	PurchaseDate:  whereHelpertime_Time{field: "\"lipid_inventory\".\"purchase_date\""},
	ExpiryDate:    whereHelpertime_Time{field: "\"lipid_inventory\".\"expiry_date\""},
	Cost:          whereHelperfloat64{field: "\"lipid_inventory\".\"cost\""},
	Weight:        whereHelperfloat64{field: "\"lipid_inventory\".\"weight\""},
	Sap:           whereHelperfloat64{field: "\"lipid_inventory\".\"sap\""},
	Naoh:          whereHelperfloat64{field: "\"lipid_inventory\".\"naoh\""},
	Koh:           whereHelperfloat64{field: "\"lipid_inventory\".\"koh\""},
	GramsPerLiter: whereHelperfloat64{field: "\"lipid_inventory\".\"grams_per_liter\""},
	LipidID:       whereHelperint{field: "\"lipid_inventory\".\"lipid_id\""},
	SupplierID:    whereHelperint{field: "\"lipid_inventory\".\"supplier_id\""},
}

// LipidInventoryRels is where relationship names are stored.
var LipidInventoryRels = struct {
	Lipid    string
	Supplier string
}{
	Lipid:    "Lipid",
	Supplier: "Supplier",
}

// lipidInventoryR is where relationships are stored.
type lipidInventoryR struct {
	Lipid    *Lipid    `boil:"Lipid" json:"Lipid" toml:"Lipid" yaml:"Lipid"`
	Supplier *Supplier `boil:"Supplier" json:"Supplier" toml:"Supplier" yaml:"Supplier"`
}

// NewStruct creates a new relationship struct
func (*lipidInventoryR) NewStruct() *lipidInventoryR {
	return &lipidInventoryR{}
}

// lipidInventoryL is where Load methods for each relationship are stored.
type lipidInventoryL struct{}

var (
	lipidInventoryAllColumns            = []string{"id", "created_at", "updated_at", "deleted_at", "purchase_date", "expiry_date", "cost", "weight", "sap", "naoh", "koh", "grams_per_liter", "lipid_id", "supplier_id"}
	lipidInventoryColumnsWithoutDefault = []string{"created_at", "updated_at", "deleted_at", "purchase_date", "expiry_date", "cost", "weight", "sap", "naoh", "koh", "grams_per_liter", "lipid_id", "supplier_id"}
	lipidInventoryColumnsWithDefault    = []string{"id"}
	lipidInventoryPrimaryKeyColumns     = []string{"id"}
)

type (
	// LipidInventorySlice is an alias for a slice of pointers to LipidInventory.
	// This should generally be used opposed to []LipidInventory.
	LipidInventorySlice []*LipidInventory
	// LipidInventoryHook is the signature for custom LipidInventory hook methods
	LipidInventoryHook func(context.Context, boil.ContextExecutor, *LipidInventory) error

	lipidInventoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	lipidInventoryType                 = reflect.TypeOf(&LipidInventory{})
	lipidInventoryMapping              = queries.MakeStructMapping(lipidInventoryType)
	lipidInventoryPrimaryKeyMapping, _ = queries.BindMapping(lipidInventoryType, lipidInventoryMapping, lipidInventoryPrimaryKeyColumns)
	lipidInventoryInsertCacheMut       sync.RWMutex
	lipidInventoryInsertCache          = make(map[string]insertCache)
	lipidInventoryUpdateCacheMut       sync.RWMutex
	lipidInventoryUpdateCache          = make(map[string]updateCache)
	lipidInventoryUpsertCacheMut       sync.RWMutex
	lipidInventoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var lipidInventoryBeforeInsertHooks []LipidInventoryHook
var lipidInventoryBeforeUpdateHooks []LipidInventoryHook
var lipidInventoryBeforeDeleteHooks []LipidInventoryHook
var lipidInventoryBeforeUpsertHooks []LipidInventoryHook

var lipidInventoryAfterInsertHooks []LipidInventoryHook
var lipidInventoryAfterSelectHooks []LipidInventoryHook
var lipidInventoryAfterUpdateHooks []LipidInventoryHook
var lipidInventoryAfterDeleteHooks []LipidInventoryHook
var lipidInventoryAfterUpsertHooks []LipidInventoryHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *LipidInventory) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lipidInventoryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *LipidInventory) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lipidInventoryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *LipidInventory) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lipidInventoryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *LipidInventory) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lipidInventoryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *LipidInventory) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lipidInventoryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *LipidInventory) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lipidInventoryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *LipidInventory) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lipidInventoryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *LipidInventory) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lipidInventoryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *LipidInventory) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range lipidInventoryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddLipidInventoryHook registers your hook function for all future operations.
func AddLipidInventoryHook(hookPoint boil.HookPoint, lipidInventoryHook LipidInventoryHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		lipidInventoryBeforeInsertHooks = append(lipidInventoryBeforeInsertHooks, lipidInventoryHook)
	case boil.BeforeUpdateHook:
		lipidInventoryBeforeUpdateHooks = append(lipidInventoryBeforeUpdateHooks, lipidInventoryHook)
	case boil.BeforeDeleteHook:
		lipidInventoryBeforeDeleteHooks = append(lipidInventoryBeforeDeleteHooks, lipidInventoryHook)
	case boil.BeforeUpsertHook:
		lipidInventoryBeforeUpsertHooks = append(lipidInventoryBeforeUpsertHooks, lipidInventoryHook)
	case boil.AfterInsertHook:
		lipidInventoryAfterInsertHooks = append(lipidInventoryAfterInsertHooks, lipidInventoryHook)
	case boil.AfterSelectHook:
		lipidInventoryAfterSelectHooks = append(lipidInventoryAfterSelectHooks, lipidInventoryHook)
	case boil.AfterUpdateHook:
		lipidInventoryAfterUpdateHooks = append(lipidInventoryAfterUpdateHooks, lipidInventoryHook)
	case boil.AfterDeleteHook:
		lipidInventoryAfterDeleteHooks = append(lipidInventoryAfterDeleteHooks, lipidInventoryHook)
	case boil.AfterUpsertHook:
		lipidInventoryAfterUpsertHooks = append(lipidInventoryAfterUpsertHooks, lipidInventoryHook)
	}
}

// One returns a single lipidInventory record from the query.
func (q lipidInventoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*LipidInventory, error) {
	o := &LipidInventory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for lipid_inventory")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all LipidInventory records from the query.
func (q lipidInventoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (LipidInventorySlice, error) {
	var o []*LipidInventory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to LipidInventory slice")
	}

	if len(lipidInventoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all LipidInventory records in the query.
func (q lipidInventoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count lipid_inventory rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q lipidInventoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if lipid_inventory exists")
	}

	return count > 0, nil
}

// Lipid pointed to by the foreign key.
func (o *LipidInventory) Lipid(mods ...qm.QueryMod) lipidQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.LipidID),
	}

	queryMods = append(queryMods, mods...)

	query := Lipids(queryMods...)
	queries.SetFrom(query.Query, "\"lipid\"")

	return query
}

// Supplier pointed to by the foreign key.
func (o *LipidInventory) Supplier(mods ...qm.QueryMod) supplierQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SupplierID),
	}

	queryMods = append(queryMods, mods...)

	query := Suppliers(queryMods...)
	queries.SetFrom(query.Query, "\"supplier\"")

	return query
}

// LoadLipid allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (lipidInventoryL) LoadLipid(ctx context.Context, e boil.ContextExecutor, singular bool, maybeLipidInventory interface{}, mods queries.Applicator) error {
	var slice []*LipidInventory
	var object *LipidInventory

	if singular {
		object = maybeLipidInventory.(*LipidInventory)
	} else {
		slice = *maybeLipidInventory.(*[]*LipidInventory)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &lipidInventoryR{}
		}
		args = append(args, object.LipidID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &lipidInventoryR{}
			}

			for _, a := range args {
				if a == obj.LipidID {
					continue Outer
				}
			}

			args = append(args, obj.LipidID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`lipid`),
		qm.WhereIn(`lipid.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Lipid")
	}

	var resultSlice []*Lipid
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Lipid")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for lipid")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for lipid")
	}

	if len(lipidInventoryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Lipid = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.LipidID == foreign.ID {
				local.R.Lipid = foreign
				break
			}
		}
	}

	return nil
}

// LoadSupplier allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (lipidInventoryL) LoadSupplier(ctx context.Context, e boil.ContextExecutor, singular bool, maybeLipidInventory interface{}, mods queries.Applicator) error {
	var slice []*LipidInventory
	var object *LipidInventory

	if singular {
		object = maybeLipidInventory.(*LipidInventory)
	} else {
		slice = *maybeLipidInventory.(*[]*LipidInventory)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &lipidInventoryR{}
		}
		args = append(args, object.SupplierID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &lipidInventoryR{}
			}

			for _, a := range args {
				if a == obj.SupplierID {
					continue Outer
				}
			}

			args = append(args, obj.SupplierID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`supplier`),
		qm.WhereIn(`supplier.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Supplier")
	}

	var resultSlice []*Supplier
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Supplier")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for supplier")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for supplier")
	}

	if len(lipidInventoryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Supplier = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SupplierID == foreign.ID {
				local.R.Supplier = foreign
				break
			}
		}
	}

	return nil
}

// SetLipid of the lipidInventory to the related item.
// Sets o.R.Lipid to related.
// Adds o to related.R.LipidInventories.
func (o *LipidInventory) SetLipid(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Lipid) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"lipid_inventory\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"lipid_id"}),
		strmangle.WhereClause("\"", "\"", 2, lipidInventoryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.LipidID = related.ID
	if o.R == nil {
		o.R = &lipidInventoryR{
			Lipid: related,
		}
	} else {
		o.R.Lipid = related
	}

	if related.R == nil {
		related.R = &lipidR{
			LipidInventories: LipidInventorySlice{o},
		}
	} else {
		related.R.LipidInventories = append(related.R.LipidInventories, o)
	}

	return nil
}

// SetSupplier of the lipidInventory to the related item.
// Sets o.R.Supplier to related.
// Adds o to related.R.LipidInventories.
func (o *LipidInventory) SetSupplier(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Supplier) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"lipid_inventory\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"supplier_id"}),
		strmangle.WhereClause("\"", "\"", 2, lipidInventoryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SupplierID = related.ID
	if o.R == nil {
		o.R = &lipidInventoryR{
			Supplier: related,
		}
	} else {
		o.R.Supplier = related
	}

	if related.R == nil {
		related.R = &supplierR{
			LipidInventories: LipidInventorySlice{o},
		}
	} else {
		related.R.LipidInventories = append(related.R.LipidInventories, o)
	}

	return nil
}

// LipidInventories retrieves all the records using an executor.
func LipidInventories(mods ...qm.QueryMod) lipidInventoryQuery {
	mods = append(mods, qm.From("\"lipid_inventory\""))
	return lipidInventoryQuery{NewQuery(mods...)}
}

// FindLipidInventory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindLipidInventory(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*LipidInventory, error) {
	lipidInventoryObj := &LipidInventory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"lipid_inventory\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, lipidInventoryObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from lipid_inventory")
	}

	return lipidInventoryObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *LipidInventory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no lipid_inventory provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lipidInventoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	lipidInventoryInsertCacheMut.RLock()
	cache, cached := lipidInventoryInsertCache[key]
	lipidInventoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			lipidInventoryAllColumns,
			lipidInventoryColumnsWithDefault,
			lipidInventoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(lipidInventoryType, lipidInventoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(lipidInventoryType, lipidInventoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"lipid_inventory\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"lipid_inventory\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into lipid_inventory")
	}

	if !cached {
		lipidInventoryInsertCacheMut.Lock()
		lipidInventoryInsertCache[key] = cache
		lipidInventoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the LipidInventory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *LipidInventory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	lipidInventoryUpdateCacheMut.RLock()
	cache, cached := lipidInventoryUpdateCache[key]
	lipidInventoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			lipidInventoryAllColumns,
			lipidInventoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update lipid_inventory, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"lipid_inventory\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, lipidInventoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(lipidInventoryType, lipidInventoryMapping, append(wl, lipidInventoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update lipid_inventory row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for lipid_inventory")
	}

	if !cached {
		lipidInventoryUpdateCacheMut.Lock()
		lipidInventoryUpdateCache[key] = cache
		lipidInventoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q lipidInventoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for lipid_inventory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for lipid_inventory")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o LipidInventorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lipidInventoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"lipid_inventory\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, lipidInventoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in lipidInventory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all lipidInventory")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *LipidInventory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no lipid_inventory provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(lipidInventoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	lipidInventoryUpsertCacheMut.RLock()
	cache, cached := lipidInventoryUpsertCache[key]
	lipidInventoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			lipidInventoryAllColumns,
			lipidInventoryColumnsWithDefault,
			lipidInventoryColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			lipidInventoryAllColumns,
			lipidInventoryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert lipid_inventory, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(lipidInventoryPrimaryKeyColumns))
			copy(conflict, lipidInventoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"lipid_inventory\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(lipidInventoryType, lipidInventoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(lipidInventoryType, lipidInventoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert lipid_inventory")
	}

	if !cached {
		lipidInventoryUpsertCacheMut.Lock()
		lipidInventoryUpsertCache[key] = cache
		lipidInventoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single LipidInventory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *LipidInventory) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no LipidInventory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), lipidInventoryPrimaryKeyMapping)
	sql := "DELETE FROM \"lipid_inventory\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from lipid_inventory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for lipid_inventory")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q lipidInventoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no lipidInventoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from lipid_inventory")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for lipid_inventory")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o LipidInventorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(lipidInventoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lipidInventoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"lipid_inventory\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lipidInventoryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from lipidInventory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for lipid_inventory")
	}

	if len(lipidInventoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *LipidInventory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindLipidInventory(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *LipidInventorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := LipidInventorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), lipidInventoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"lipid_inventory\".* FROM \"lipid_inventory\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, lipidInventoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in LipidInventorySlice")
	}

	*o = slice

	return nil
}

// LipidInventoryExists checks if the LipidInventory row exists.
func LipidInventoryExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"lipid_inventory\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if lipid_inventory exists")
	}

	return exists, nil
}
