// Code generated by SQLBoiler 4.2.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// RecipeBatchAdditive is an object representing the database table.
type RecipeBatchAdditive struct {
	ID         int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	CreatedAt  time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt  null.Time `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`
	Weight     float64   `boil:"weight" json:"weight" toml:"weight" yaml:"weight"`
	Cost       float64   `boil:"cost" json:"cost" toml:"cost" yaml:"cost"`
	AdditiveID int       `boil:"additive_id" json:"additive_id" toml:"additive_id" yaml:"additive_id"`
	BatchID    int       `boil:"batch_id" json:"batch_id" toml:"batch_id" yaml:"batch_id"`

	R *recipeBatchAdditiveR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L recipeBatchAdditiveL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var RecipeBatchAdditiveColumns = struct {
	ID         string
	CreatedAt  string
	UpdatedAt  string
	DeletedAt  string
	Weight     string
	Cost       string
	AdditiveID string
	BatchID    string
}{
	ID:         "id",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
	DeletedAt:  "deleted_at",
	Weight:     "weight",
	Cost:       "cost",
	AdditiveID: "additive_id",
	BatchID:    "batch_id",
}

// Generated where

var RecipeBatchAdditiveWhere = struct {
	ID         whereHelperint
	CreatedAt  whereHelpertime_Time
	UpdatedAt  whereHelpertime_Time
	DeletedAt  whereHelpernull_Time
	Weight     whereHelperfloat64
	Cost       whereHelperfloat64
	AdditiveID whereHelperint
	BatchID    whereHelperint
}{
	ID:         whereHelperint{field: "\"recipe_batch_additive\".\"id\""},
	CreatedAt:  whereHelpertime_Time{field: "\"recipe_batch_additive\".\"created_at\""},
	UpdatedAt:  whereHelpertime_Time{field: "\"recipe_batch_additive\".\"updated_at\""},
	DeletedAt:  whereHelpernull_Time{field: "\"recipe_batch_additive\".\"deleted_at\""},
	Weight:     whereHelperfloat64{field: "\"recipe_batch_additive\".\"weight\""},
	Cost:       whereHelperfloat64{field: "\"recipe_batch_additive\".\"cost\""},
	AdditiveID: whereHelperint{field: "\"recipe_batch_additive\".\"additive_id\""},
	BatchID:    whereHelperint{field: "\"recipe_batch_additive\".\"batch_id\""},
}

// RecipeBatchAdditiveRels is where relationship names are stored.
var RecipeBatchAdditiveRels = struct {
	Additive string
	Batch    string
}{
	Additive: "Additive",
	Batch:    "Batch",
}

// recipeBatchAdditiveR is where relationships are stored.
type recipeBatchAdditiveR struct {
	Additive *Additive    `boil:"Additive" json:"Additive" toml:"Additive" yaml:"Additive"`
	Batch    *RecipeBatch `boil:"Batch" json:"Batch" toml:"Batch" yaml:"Batch"`
}

// NewStruct creates a new relationship struct
func (*recipeBatchAdditiveR) NewStruct() *recipeBatchAdditiveR {
	return &recipeBatchAdditiveR{}
}

// recipeBatchAdditiveL is where Load methods for each relationship are stored.
type recipeBatchAdditiveL struct{}

var (
	recipeBatchAdditiveAllColumns            = []string{"id", "created_at", "updated_at", "deleted_at", "weight", "cost", "additive_id", "batch_id"}
	recipeBatchAdditiveColumnsWithoutDefault = []string{"created_at", "updated_at", "deleted_at", "weight", "cost", "additive_id", "batch_id"}
	recipeBatchAdditiveColumnsWithDefault    = []string{"id"}
	recipeBatchAdditivePrimaryKeyColumns     = []string{"id"}
)

type (
	// RecipeBatchAdditiveSlice is an alias for a slice of pointers to RecipeBatchAdditive.
	// This should generally be used opposed to []RecipeBatchAdditive.
	RecipeBatchAdditiveSlice []*RecipeBatchAdditive
	// RecipeBatchAdditiveHook is the signature for custom RecipeBatchAdditive hook methods
	RecipeBatchAdditiveHook func(context.Context, boil.ContextExecutor, *RecipeBatchAdditive) error

	recipeBatchAdditiveQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	recipeBatchAdditiveType                 = reflect.TypeOf(&RecipeBatchAdditive{})
	recipeBatchAdditiveMapping              = queries.MakeStructMapping(recipeBatchAdditiveType)
	recipeBatchAdditivePrimaryKeyMapping, _ = queries.BindMapping(recipeBatchAdditiveType, recipeBatchAdditiveMapping, recipeBatchAdditivePrimaryKeyColumns)
	recipeBatchAdditiveInsertCacheMut       sync.RWMutex
	recipeBatchAdditiveInsertCache          = make(map[string]insertCache)
	recipeBatchAdditiveUpdateCacheMut       sync.RWMutex
	recipeBatchAdditiveUpdateCache          = make(map[string]updateCache)
	recipeBatchAdditiveUpsertCacheMut       sync.RWMutex
	recipeBatchAdditiveUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var recipeBatchAdditiveBeforeInsertHooks []RecipeBatchAdditiveHook
var recipeBatchAdditiveBeforeUpdateHooks []RecipeBatchAdditiveHook
var recipeBatchAdditiveBeforeDeleteHooks []RecipeBatchAdditiveHook
var recipeBatchAdditiveBeforeUpsertHooks []RecipeBatchAdditiveHook

var recipeBatchAdditiveAfterInsertHooks []RecipeBatchAdditiveHook
var recipeBatchAdditiveAfterSelectHooks []RecipeBatchAdditiveHook
var recipeBatchAdditiveAfterUpdateHooks []RecipeBatchAdditiveHook
var recipeBatchAdditiveAfterDeleteHooks []RecipeBatchAdditiveHook
var recipeBatchAdditiveAfterUpsertHooks []RecipeBatchAdditiveHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *RecipeBatchAdditive) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range recipeBatchAdditiveBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *RecipeBatchAdditive) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range recipeBatchAdditiveBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *RecipeBatchAdditive) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range recipeBatchAdditiveBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *RecipeBatchAdditive) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range recipeBatchAdditiveBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *RecipeBatchAdditive) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range recipeBatchAdditiveAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *RecipeBatchAdditive) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range recipeBatchAdditiveAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *RecipeBatchAdditive) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range recipeBatchAdditiveAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *RecipeBatchAdditive) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range recipeBatchAdditiveAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *RecipeBatchAdditive) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range recipeBatchAdditiveAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddRecipeBatchAdditiveHook registers your hook function for all future operations.
func AddRecipeBatchAdditiveHook(hookPoint boil.HookPoint, recipeBatchAdditiveHook RecipeBatchAdditiveHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		recipeBatchAdditiveBeforeInsertHooks = append(recipeBatchAdditiveBeforeInsertHooks, recipeBatchAdditiveHook)
	case boil.BeforeUpdateHook:
		recipeBatchAdditiveBeforeUpdateHooks = append(recipeBatchAdditiveBeforeUpdateHooks, recipeBatchAdditiveHook)
	case boil.BeforeDeleteHook:
		recipeBatchAdditiveBeforeDeleteHooks = append(recipeBatchAdditiveBeforeDeleteHooks, recipeBatchAdditiveHook)
	case boil.BeforeUpsertHook:
		recipeBatchAdditiveBeforeUpsertHooks = append(recipeBatchAdditiveBeforeUpsertHooks, recipeBatchAdditiveHook)
	case boil.AfterInsertHook:
		recipeBatchAdditiveAfterInsertHooks = append(recipeBatchAdditiveAfterInsertHooks, recipeBatchAdditiveHook)
	case boil.AfterSelectHook:
		recipeBatchAdditiveAfterSelectHooks = append(recipeBatchAdditiveAfterSelectHooks, recipeBatchAdditiveHook)
	case boil.AfterUpdateHook:
		recipeBatchAdditiveAfterUpdateHooks = append(recipeBatchAdditiveAfterUpdateHooks, recipeBatchAdditiveHook)
	case boil.AfterDeleteHook:
		recipeBatchAdditiveAfterDeleteHooks = append(recipeBatchAdditiveAfterDeleteHooks, recipeBatchAdditiveHook)
	case boil.AfterUpsertHook:
		recipeBatchAdditiveAfterUpsertHooks = append(recipeBatchAdditiveAfterUpsertHooks, recipeBatchAdditiveHook)
	}
}

// One returns a single recipeBatchAdditive record from the query.
func (q recipeBatchAdditiveQuery) One(ctx context.Context, exec boil.ContextExecutor) (*RecipeBatchAdditive, error) {
	o := &RecipeBatchAdditive{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for recipe_batch_additive")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all RecipeBatchAdditive records from the query.
func (q recipeBatchAdditiveQuery) All(ctx context.Context, exec boil.ContextExecutor) (RecipeBatchAdditiveSlice, error) {
	var o []*RecipeBatchAdditive

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to RecipeBatchAdditive slice")
	}

	if len(recipeBatchAdditiveAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all RecipeBatchAdditive records in the query.
func (q recipeBatchAdditiveQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count recipe_batch_additive rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q recipeBatchAdditiveQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if recipe_batch_additive exists")
	}

	return count > 0, nil
}

// Additive pointed to by the foreign key.
func (o *RecipeBatchAdditive) Additive(mods ...qm.QueryMod) additiveQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AdditiveID),
	}

	queryMods = append(queryMods, mods...)

	query := Additives(queryMods...)
	queries.SetFrom(query.Query, "\"additive\"")

	return query
}

// Batch pointed to by the foreign key.
func (o *RecipeBatchAdditive) Batch(mods ...qm.QueryMod) recipeBatchQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.BatchID),
	}

	queryMods = append(queryMods, mods...)

	query := RecipeBatches(queryMods...)
	queries.SetFrom(query.Query, "\"recipe_batch\"")

	return query
}

// LoadAdditive allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (recipeBatchAdditiveL) LoadAdditive(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRecipeBatchAdditive interface{}, mods queries.Applicator) error {
	var slice []*RecipeBatchAdditive
	var object *RecipeBatchAdditive

	if singular {
		object = maybeRecipeBatchAdditive.(*RecipeBatchAdditive)
	} else {
		slice = *maybeRecipeBatchAdditive.(*[]*RecipeBatchAdditive)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &recipeBatchAdditiveR{}
		}
		args = append(args, object.AdditiveID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &recipeBatchAdditiveR{}
			}

			for _, a := range args {
				if a == obj.AdditiveID {
					continue Outer
				}
			}

			args = append(args, obj.AdditiveID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`additive`),
		qm.WhereIn(`additive.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Additive")
	}

	var resultSlice []*Additive
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Additive")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for additive")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for additive")
	}

	if len(recipeBatchAdditiveAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Additive = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AdditiveID == foreign.ID {
				local.R.Additive = foreign
				break
			}
		}
	}

	return nil
}

// LoadBatch allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (recipeBatchAdditiveL) LoadBatch(ctx context.Context, e boil.ContextExecutor, singular bool, maybeRecipeBatchAdditive interface{}, mods queries.Applicator) error {
	var slice []*RecipeBatchAdditive
	var object *RecipeBatchAdditive

	if singular {
		object = maybeRecipeBatchAdditive.(*RecipeBatchAdditive)
	} else {
		slice = *maybeRecipeBatchAdditive.(*[]*RecipeBatchAdditive)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &recipeBatchAdditiveR{}
		}
		args = append(args, object.BatchID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &recipeBatchAdditiveR{}
			}

			for _, a := range args {
				if a == obj.BatchID {
					continue Outer
				}
			}

			args = append(args, obj.BatchID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`recipe_batch`),
		qm.WhereIn(`recipe_batch.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load RecipeBatch")
	}

	var resultSlice []*RecipeBatch
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice RecipeBatch")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for recipe_batch")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for recipe_batch")
	}

	if len(recipeBatchAdditiveAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Batch = foreign
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BatchID == foreign.ID {
				local.R.Batch = foreign
				break
			}
		}
	}

	return nil
}

// SetAdditive of the recipeBatchAdditive to the related item.
// Sets o.R.Additive to related.
// Adds o to related.R.RecipeBatchAdditive.
func (o *RecipeBatchAdditive) SetAdditive(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Additive) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"recipe_batch_additive\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"additive_id"}),
		strmangle.WhereClause("\"", "\"", 2, recipeBatchAdditivePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AdditiveID = related.ID
	if o.R == nil {
		o.R = &recipeBatchAdditiveR{
			Additive: related,
		}
	} else {
		o.R.Additive = related
	}

	if related.R == nil {
		related.R = &additiveR{
			RecipeBatchAdditive: o,
		}
	} else {
		related.R.RecipeBatchAdditive = o
	}

	return nil
}

// SetBatch of the recipeBatchAdditive to the related item.
// Sets o.R.Batch to related.
// Adds o to related.R.BatchRecipeBatchAdditives.
func (o *RecipeBatchAdditive) SetBatch(ctx context.Context, exec boil.ContextExecutor, insert bool, related *RecipeBatch) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"recipe_batch_additive\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"batch_id"}),
		strmangle.WhereClause("\"", "\"", 2, recipeBatchAdditivePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BatchID = related.ID
	if o.R == nil {
		o.R = &recipeBatchAdditiveR{
			Batch: related,
		}
	} else {
		o.R.Batch = related
	}

	if related.R == nil {
		related.R = &recipeBatchR{
			BatchRecipeBatchAdditives: RecipeBatchAdditiveSlice{o},
		}
	} else {
		related.R.BatchRecipeBatchAdditives = append(related.R.BatchRecipeBatchAdditives, o)
	}

	return nil
}

// RecipeBatchAdditives retrieves all the records using an executor.
func RecipeBatchAdditives(mods ...qm.QueryMod) recipeBatchAdditiveQuery {
	mods = append(mods, qm.From("\"recipe_batch_additive\""))
	return recipeBatchAdditiveQuery{NewQuery(mods...)}
}

// FindRecipeBatchAdditive retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindRecipeBatchAdditive(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*RecipeBatchAdditive, error) {
	recipeBatchAdditiveObj := &RecipeBatchAdditive{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"recipe_batch_additive\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, recipeBatchAdditiveObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from recipe_batch_additive")
	}

	return recipeBatchAdditiveObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *RecipeBatchAdditive) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no recipe_batch_additive provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(recipeBatchAdditiveColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	recipeBatchAdditiveInsertCacheMut.RLock()
	cache, cached := recipeBatchAdditiveInsertCache[key]
	recipeBatchAdditiveInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			recipeBatchAdditiveAllColumns,
			recipeBatchAdditiveColumnsWithDefault,
			recipeBatchAdditiveColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(recipeBatchAdditiveType, recipeBatchAdditiveMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(recipeBatchAdditiveType, recipeBatchAdditiveMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"recipe_batch_additive\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"recipe_batch_additive\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into recipe_batch_additive")
	}

	if !cached {
		recipeBatchAdditiveInsertCacheMut.Lock()
		recipeBatchAdditiveInsertCache[key] = cache
		recipeBatchAdditiveInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the RecipeBatchAdditive.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *RecipeBatchAdditive) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	recipeBatchAdditiveUpdateCacheMut.RLock()
	cache, cached := recipeBatchAdditiveUpdateCache[key]
	recipeBatchAdditiveUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			recipeBatchAdditiveAllColumns,
			recipeBatchAdditivePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update recipe_batch_additive, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"recipe_batch_additive\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, recipeBatchAdditivePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(recipeBatchAdditiveType, recipeBatchAdditiveMapping, append(wl, recipeBatchAdditivePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update recipe_batch_additive row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for recipe_batch_additive")
	}

	if !cached {
		recipeBatchAdditiveUpdateCacheMut.Lock()
		recipeBatchAdditiveUpdateCache[key] = cache
		recipeBatchAdditiveUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q recipeBatchAdditiveQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for recipe_batch_additive")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for recipe_batch_additive")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o RecipeBatchAdditiveSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), recipeBatchAdditivePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"recipe_batch_additive\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, recipeBatchAdditivePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in recipeBatchAdditive slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all recipeBatchAdditive")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *RecipeBatchAdditive) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no recipe_batch_additive provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(recipeBatchAdditiveColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	recipeBatchAdditiveUpsertCacheMut.RLock()
	cache, cached := recipeBatchAdditiveUpsertCache[key]
	recipeBatchAdditiveUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			recipeBatchAdditiveAllColumns,
			recipeBatchAdditiveColumnsWithDefault,
			recipeBatchAdditiveColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			recipeBatchAdditiveAllColumns,
			recipeBatchAdditivePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert recipe_batch_additive, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(recipeBatchAdditivePrimaryKeyColumns))
			copy(conflict, recipeBatchAdditivePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"recipe_batch_additive\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(recipeBatchAdditiveType, recipeBatchAdditiveMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(recipeBatchAdditiveType, recipeBatchAdditiveMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert recipe_batch_additive")
	}

	if !cached {
		recipeBatchAdditiveUpsertCacheMut.Lock()
		recipeBatchAdditiveUpsertCache[key] = cache
		recipeBatchAdditiveUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single RecipeBatchAdditive record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *RecipeBatchAdditive) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no RecipeBatchAdditive provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), recipeBatchAdditivePrimaryKeyMapping)
	sql := "DELETE FROM \"recipe_batch_additive\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from recipe_batch_additive")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for recipe_batch_additive")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q recipeBatchAdditiveQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no recipeBatchAdditiveQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from recipe_batch_additive")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for recipe_batch_additive")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o RecipeBatchAdditiveSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(recipeBatchAdditiveBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), recipeBatchAdditivePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"recipe_batch_additive\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, recipeBatchAdditivePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from recipeBatchAdditive slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for recipe_batch_additive")
	}

	if len(recipeBatchAdditiveAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *RecipeBatchAdditive) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindRecipeBatchAdditive(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *RecipeBatchAdditiveSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := RecipeBatchAdditiveSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), recipeBatchAdditivePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"recipe_batch_additive\".* FROM \"recipe_batch_additive\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, recipeBatchAdditivePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in RecipeBatchAdditiveSlice")
	}

	*o = slice

	return nil
}

// RecipeBatchAdditiveExists checks if the RecipeBatchAdditive row exists.
func RecipeBatchAdditiveExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"recipe_batch_additive\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if recipe_batch_additive exists")
	}

	return exists, nil
}
