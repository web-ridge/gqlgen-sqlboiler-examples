// Code generated by SQLBoiler 4.2.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDjangoSessions(t *testing.T) {
	t.Parallel()

	query := DjangoSessions()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDjangoSessionsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDjangoSessionsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := DjangoSessions().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDjangoSessionsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DjangoSessionSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDjangoSessionsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DjangoSessionExists(ctx, tx, o.SessionKey)
	if err != nil {
		t.Errorf("Unable to check if DjangoSession exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DjangoSessionExists to return true, but got false.")
	}
}

func testDjangoSessionsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	djangoSessionFound, err := FindDjangoSession(ctx, tx, o.SessionKey)
	if err != nil {
		t.Error(err)
	}

	if djangoSessionFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDjangoSessionsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = DjangoSessions().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testDjangoSessionsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := DjangoSessions().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDjangoSessionsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	djangoSessionOne := &DjangoSession{}
	djangoSessionTwo := &DjangoSession{}
	if err = randomize.Struct(seed, djangoSessionOne, djangoSessionDBTypes, false, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}
	if err = randomize.Struct(seed, djangoSessionTwo, djangoSessionDBTypes, false, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = djangoSessionOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = djangoSessionTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DjangoSessions().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDjangoSessionsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	djangoSessionOne := &DjangoSession{}
	djangoSessionTwo := &DjangoSession{}
	if err = randomize.Struct(seed, djangoSessionOne, djangoSessionDBTypes, false, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}
	if err = randomize.Struct(seed, djangoSessionTwo, djangoSessionDBTypes, false, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = djangoSessionOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = djangoSessionTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func djangoSessionBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *DjangoSession) error {
	*o = DjangoSession{}
	return nil
}

func djangoSessionAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *DjangoSession) error {
	*o = DjangoSession{}
	return nil
}

func djangoSessionAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *DjangoSession) error {
	*o = DjangoSession{}
	return nil
}

func djangoSessionBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DjangoSession) error {
	*o = DjangoSession{}
	return nil
}

func djangoSessionAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DjangoSession) error {
	*o = DjangoSession{}
	return nil
}

func djangoSessionBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DjangoSession) error {
	*o = DjangoSession{}
	return nil
}

func djangoSessionAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DjangoSession) error {
	*o = DjangoSession{}
	return nil
}

func djangoSessionBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DjangoSession) error {
	*o = DjangoSession{}
	return nil
}

func djangoSessionAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DjangoSession) error {
	*o = DjangoSession{}
	return nil
}

func testDjangoSessionsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &DjangoSession{}
	o := &DjangoSession{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, false); err != nil {
		t.Errorf("Unable to randomize DjangoSession object: %s", err)
	}

	AddDjangoSessionHook(boil.BeforeInsertHook, djangoSessionBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	djangoSessionBeforeInsertHooks = []DjangoSessionHook{}

	AddDjangoSessionHook(boil.AfterInsertHook, djangoSessionAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	djangoSessionAfterInsertHooks = []DjangoSessionHook{}

	AddDjangoSessionHook(boil.AfterSelectHook, djangoSessionAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	djangoSessionAfterSelectHooks = []DjangoSessionHook{}

	AddDjangoSessionHook(boil.BeforeUpdateHook, djangoSessionBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	djangoSessionBeforeUpdateHooks = []DjangoSessionHook{}

	AddDjangoSessionHook(boil.AfterUpdateHook, djangoSessionAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	djangoSessionAfterUpdateHooks = []DjangoSessionHook{}

	AddDjangoSessionHook(boil.BeforeDeleteHook, djangoSessionBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	djangoSessionBeforeDeleteHooks = []DjangoSessionHook{}

	AddDjangoSessionHook(boil.AfterDeleteHook, djangoSessionAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	djangoSessionAfterDeleteHooks = []DjangoSessionHook{}

	AddDjangoSessionHook(boil.BeforeUpsertHook, djangoSessionBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	djangoSessionBeforeUpsertHooks = []DjangoSessionHook{}

	AddDjangoSessionHook(boil.AfterUpsertHook, djangoSessionAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	djangoSessionAfterUpsertHooks = []DjangoSessionHook{}
}

func testDjangoSessionsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDjangoSessionsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(djangoSessionColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDjangoSessionsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDjangoSessionsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DjangoSessionSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDjangoSessionsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DjangoSessions().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	djangoSessionDBTypes = map[string]string{`SessionKey`: `character varying`, `SessionData`: `text`, `ExpireDate`: `timestamp with time zone`}
	_                    = bytes.MinRead
)

func testDjangoSessionsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(djangoSessionPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(djangoSessionAllColumns) == len(djangoSessionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDjangoSessionsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(djangoSessionAllColumns) == len(djangoSessionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DjangoSession{}
	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, djangoSessionDBTypes, true, djangoSessionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(djangoSessionAllColumns, djangoSessionPrimaryKeyColumns) {
		fields = djangoSessionAllColumns
	} else {
		fields = strmangle.SetComplement(
			djangoSessionAllColumns,
			djangoSessionPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DjangoSessionSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDjangoSessionsUpsert(t *testing.T) {
	t.Parallel()

	if len(djangoSessionAllColumns) == len(djangoSessionPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := DjangoSession{}
	if err = randomize.Struct(seed, &o, djangoSessionDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DjangoSession: %s", err)
	}

	count, err := DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, djangoSessionDBTypes, false, djangoSessionPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DjangoSession struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DjangoSession: %s", err)
	}

	count, err = DjangoSessions().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
